namespace = ottoman_mamluk_war_events
normal_or_historical_nations = yes

# Collapse of the Mamluk Sultanate

country_event = {
	id = ottoman_mamluk_war_events.3
	title = ottoman_mamluk_war_events.3.t
	desc = ottoman_mamluk_war_events.3.d
	picture = BATTLE_eventPicture

	fire_only_once = yes
	major = yes

	mean_time_to_happen = { days = 10 }
	
	trigger = {
		is_great_power = yes
		is_in_war = { defender_leader = MAM }
		OR = {
			NOT = { has_dlc = "Domination" }
			mission_completed = tur_defeat_the_mamluks
		}
		OR = {
			tag = TUR
			tag = BYZ
			tag = LAE
			tag = RUM
			tag = ROM
		}
		OR = {
			MAM = { capital_scope = { controlled_by = ROOT } }
			MAM = { capital_scope = { controller = { is_subject_of = ROOT } } }
		}
		MAM = {
			OR = {
				has_country_modifier = mam_turmoil
				NOT = { owns = 361 } ## cairo ##
				AND = {
					ai = yes
					is_year = 1550
				}
			}
		}
		NOT = {
			is_in_war = {
				casus_belli = cb_ottoman_invasion
				defender_leader = MAM
			}
		}
		OR = {
			MAM = { NOT = { owns = 361 } } ## cairo ##
			war_score_against = {
				who = MAM
				value = 50
			}
			AND = {
				ai = yes
				is_year = 1540
			}
		}
        OR = {
        	NOT = { tag = TUR }
        	NOT = { has_dlc = "Domination" }
        	AND = {
        		has_global_flag = tur_has_taken_defeat_mamluks_mission
        		has_country_flag = tur_decided_to_go_eyalet
        	}
        	AND = {
        		is_year = 1500
        		NOT = { is_year = 1600 }
        	}
        }
	}

	option = {
		name = ottoman_mamluk_war_events.3.b # Perhaps we and the Mamluks can work together...
		ai_chance = {
			factor = 1
			modifier = {
				factor = 0.5
				MAM = {
					ai = yes
					was_player = no
				}
			}
			modifier = {
				factor = 0
				MAM = {
					num_of_owned_provinces_with = {
						value = 2
						OR = {
							region = anatolia_region
							region = mashriq_region
						}
					}
				}
			}
		}
		hidden_effect = {
			white_peace = MAM
			every_country = {
				limit = { is_subject_of = MAM }
				ROOT = { vassalize = PREV }
			}
			egypt_region = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			if = {
				limit = { religion_group = muslim }
				add_accepted_culture = bedouin_arabic
				add_accepted_culture = hejazi_culture
				add_accepted_culture = al_misr_arabic
				tabuk_area = {
					limit = {
						NOT = { is_core = ROOT }
						NOT = { is_permanent_claim = ROOT }
					}
					add_permanent_claim = ROOT
				}
				medina_area = {
					limit = {
						NOT = { is_core = ROOT }
						NOT = { is_permanent_claim = ROOT }
					}
					add_permanent_claim = ROOT
				}
				mecca_area = {
					limit = {
						NOT = { is_core = ROOT }
						NOT = { is_permanent_claim = ROOT }
					}
					add_permanent_claim = ROOT
				}
				asir_area = {
					limit = {
						NOT = { is_core = ROOT }
						NOT = { is_permanent_claim = ROOT }
					}
					add_permanent_claim = ROOT
				}
				tihama_al_yemen_area = {
					limit = {
						NOT = { is_core = ROOT }
						NOT = { is_permanent_claim = ROOT }
					}
					add_permanent_claim = ROOT
				}
				upper_yemen_area = {
					limit = {
						NOT = { is_core = ROOT }
						NOT = { is_permanent_claim = ROOT }
					}
					add_permanent_claim = ROOT
				}
			}
		}
		MAM = {
			change_tag = EGY
			change_government = monarchy
			remove_government_reform = mamluk_government
		}
		create_march = EGY
		reverse_add_opinion = {
			who = EGY
			modifier = tur_allowed_autonomy
		}
		add_country_modifier = {
			name = tur_levantine_eyalets
			duration = -1
		}
		remove_historical_rival = EGY
		EGY = {
			remove_historical_rival = ROOT
			add_liberty_desire = -100			
		}
	}

	option = {
		name = ottoman_mamluk_war_events.3.c # Slow and steady wins the race...
		ai_chance = {
			factor = 0
		}
		add_legitimacy = 10
	}
}

# Removes modifier

country_event = {
	id = ottoman_mamluk_war_events.4
	title = ottoman_mamluk_war_events.4.t
	desc = ottoman_mamluk_war_events.4.d
	picture = BATTLE_eventPicture

	fire_only_once = yes
	hidden = yes

	trigger = {
		has_country_flag = tur_annexed_mamluks
		absolutism = 80
	}

	option = {
		name = ottoman_mamluk_war_events.4.a # Disastrous!
		every_province = {
			limit = {
				has_province_modifier = tur_mamluk_control
			}
			remove_province_modifier = tur_mamluk_control
		}
	}
}